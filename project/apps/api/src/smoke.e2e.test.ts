import { describe, it } from "bun:test";

describe.skip("backend e2e", () => {
  it("placeholder for real end-to-end tests with external services", () => {
    // Intentionally skipped until a dedicated real-provider test environment exists.
  });
});
