FROM oven/bun:1.3.14

WORKDIR /app

ENV NODE_ENV=production
ENV PORT=3000

COPY . .

RUN cd project && bun install --frozen-lockfile
RUN cd project && bun run build:web

EXPOSE 3000

CMD ["bun", "project/apps/api/src/index.ts"]
