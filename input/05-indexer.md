Le frontend devrait accepter les formats de documents suivant pour l'indexation :

- Fichier textes, markdown

faire une interface qui permet de glisser deposer les fichiers.
on doit pouvoir mettre plusieurs fichiers a la fois.

On doit pouvoir aussi copier coller du texte source (et indiquer le nom du doc)
